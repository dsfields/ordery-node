{
  "name": "ordery",
  "longName": "ordery",
  "description": "Create order-by expressions for Node.js applications.",
  "version": "1.0.1",
  "dependencies": {
    "elv": "^2.0.0"
  },
  "devDependencies": {
    "chai": "~3.5.0",
    "eslint": "^4.9.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.8.0",
    "mocha": "^3.2.0",
    "nyc": "^10.2.0"
  },
  "keywords": [
    "order",
    "by",
    "expression",
    "json",
    "pointer"
  ],
  "license": "MIT",
  "main": "./lib",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/dsfields/ordery-node"
  },
  "scripts": {
    "build": "npm test && npm run lint",
    "lint": "eslint ./*",
    "test": "NODE_ENV=test nyc --reporter=lcov  --reporter=text mocha --recursive ./test/unit"
  }
}
